# Application DTOs - Availability Service

Esta carpeta contiene los Data Transfer Objects (DTOs) de la capa de aplicación.

## Propósito

Los DTOs de aplicación se utilizan para transferir datos entre handlers, servicios y comandos/queries.

## Ejemplos

- `CreateReservationDto` - DTO interno para crear reservas
- `UpdateReservationDto` - DTO interno para actualizar reservas
- `ReservationResponseDto` - DTO de respuesta con datos de reserva
- `AvailabilityDto` - DTO con información de disponibilidad
