# Google OAuth2 Configuration
# Obtener estas credenciales en: https://console.cloud.google.com/apis/credentials

# Client ID de Google OAuth2
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Client Secret de Google OAuth2
GOOGLE_CLIENT_SECRET=your-google-client-secret

# URL de callback después de autenticación
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/oauth/google/callback

# Dominios permitidos para SSO (separados por coma)
GOOGLE_ALLOWED_DOMAINS=ufps.edu.co

# URL del frontend para redirección después de SSO
FRONTEND_URL=http://localhost:3000

# JWT Configuration (ya existentes)
JWT_SECRET=bookly-secret-key-change-in-production
JWT_EXPIRATION=1d
JWT_REFRESH_EXPIRATION=7d

# MongoDB Configuration (ya existentes)
MONGODB_URI=mongodb://localhost:27017/bookly-auth

# Redis Configuration (ya existentes)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Server Configuration
PORT=3001
NODE_ENV=development
