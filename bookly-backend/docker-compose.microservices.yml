version: "3.8"

services:
  # ===============================================
  # API GATEWAY
  # ===============================================
  api-gateway:
    build:
      context: .
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.gateway
    image: bookly-backend-api-gateway:latest
    container_name: bookly-backend-api-gateway
    ports:
      - "3000:3000"
    env_file:
      - ./apps/api-gateway/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

  # ===============================================
  # AUTH SERVICE
  # ===============================================
  auth-service:
    build:
      context: .
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.auth
    image: bookly-backend-auth-service:latest
    container_name: bookly-backend-auth-service
    ports:
      - "3001:3001"
    env_file:
      - ./apps/auth-service/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

  # ===============================================
  # RESOURCES SERVICE
  # ===============================================
  resources-service:
    build:
      context: .
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.resources
    image: bookly-backend-resources-service:latest
    container_name: bookly-backend-resources-service
    ports:
      - "3002:3002"
    env_file:
      - ./apps/resources-service/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

  # ===============================================
  # AVAILABILITY SERVICE
  # ===============================================
  availability-service:
    build:
      context: .
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.availability
    image: bookly-backend-availability-service:latest
    container_name: bookly-backend-availability-service
    ports:
      - "3003:3003"
    env_file:
      - ./apps/availability-service/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

  # ===============================================
  # STOCKPILE SERVICE
  # ===============================================
  stockpile-service:
    build:
      context: .
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.stockpile
    image: bookly-backend-stockpile-service:latest
    container_name: bookly-backend-stockpile-service
    ports:
      - "3004:3004"
    env_file:
      - ./apps/stockpile-service/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

  # ===============================================
  # REPORTS SERVICE
  # ===============================================
  reports-service:
    build:
      context: .
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.reports
    image: bookly-backend-reports-service:latest
    container_name: bookly-backend-reports-service
    ports:
      - "3005:3005"
    env_file:
      - ./apps/reports-service/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

  # ===============================================
  # FRONTEND - BOOKLY WEB
  # ===============================================
  bookly-web:
    build:
      context: ../bookly-frontend
      dockerfile: ../ci-cd/bookly-backend/dockerfiles/Dockerfile.web
    image: bookly-frontend:latest
    container_name: bookly-frontend
    ports:
      - "4200:4200"
    env_file:
      - ../bookly-frontend/.env
    networks:
      - bookly-backend-network
    restart: unless-stopped

networks:
  bookly-backend-network:
    driver: bridge
