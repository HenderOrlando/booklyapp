name: Auth Service - Build and Deploy

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'bookly-mock/apps/auth-service/**'
      - 'bookly-mock/libs/**'
      - 'ci-cd/bookly-mock/dockerfiles/Dockerfile.auth'
      - '.github/workflows/auth-service.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/build-and-push-image.yml
    with:
      service-name: auth-service
      dockerfile-path: ci-cd/bookly-mock/dockerfiles/Dockerfile.auth
      context-path: bookly-mock
      image-name: bookly-auth-service

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Deploy Auth Service via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            # Login to GitHub Container Registry
            echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull the latest image
            # Set image variables
            REGISTRY="ghcr.io/${{ github.repository_owner }}"
            TAG="${{ github.ref_name }}-${{ github.sha }}"
            IMAGE="$REGISTRY/bookly-auth-service:$TAG"
            docker pull "$IMAGE"

            # Stop and remove existing container
            docker stop bookly-auth-service || true
            docker rm bookly-auth-service || true

            # Run the new container
            docker run -d \
              --name bookly-auth-service \
              --restart unless-stopped \
              -p 3001:3001 \
              --network bookly-network \
              --env-file /opt/bookly/.env.auth-service \
              "$IMAGE"

            # Clean up old images
            docker image prune -f
